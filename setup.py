from setuptools import find_packages, setup

setup(
    name="src",
    version="0.0.1",
    author="pramod",
    # author_email="cloud@ineuron.ai",
    packages=find_packages(),
    install_requires=[],
)

